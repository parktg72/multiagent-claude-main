# Worker Input

Use for `codex-sol` only. State objective, allowed target paths, and verification
commands. Dispatcher passes this file through stdin and never copies it into log.

Do not include secrets, provider credentials, destructive instructions, or another
reviewer's verdict.
